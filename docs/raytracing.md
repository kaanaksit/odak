# Raytracing 🔍
Odak provides a set of functions that implement methods used in raytracing.
The ones implemented in `Numpy`, such as `odak.raytracing`, are not differentiable.
However, the ones implemented in `Torch`, such as `odak.learn.raytracing`, are differentiable.

